function whyCreated() {
  const section = document.querySelector('[anm-section=why-created]')

  if (section) {
    const headline = section.querySelector('[anm-scroll-text=headline]')

    const tl = gsap.timeline({
      defaults: { duration: 1, ease: 'expo.out' },
    })
  }
}
